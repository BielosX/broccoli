pluginManagement {
    repositories {
        maven {
            url repositoryUrl
            credentials {
                username repositoryUser
                password repositoryPassword
            }
        }
    }
}

rootProject.name = 'repo-manager'
